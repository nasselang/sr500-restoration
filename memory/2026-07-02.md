### 2026-07-02

- Finalized setup for the microsoft-foundry/gpt-5.4 model for image analysis in OpenClaw.
- Key changes:
  - Added an `apiKey` field directly into the `models.providers.microsoft-foundry` section of `openclaw.json` as an inline authentication method.
  - Confirmed the key from `AZURE_OPENAI_API_KEY` environment variable and directly embedded it in config for compatibility (gateway process context vs. environment variable inconsistency suspected).
  - Coordinated API endpoint settings and ensured the correct Foundry baseUrl: `https://adm-u34879-johnny-resource.openai.azure.com/openai/v1`.
  - Troubleshooted repeated 401 errors; confirmed key mismatches and provider schema issues initially caused failures.
- User (Johnny) restarted gateway after adjustments.
- Awaiting successful confirmation of image analysis using a test SR500 project image.
- Maintained distinction between DeepSeek for conversation model and Foundry for imageModel integration (user concern).